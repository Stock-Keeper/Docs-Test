# Phase Control 시스템 가이드

> **최종 업데이트**: 2026-02-01  
> **상태**: ✅ 구현 완료

## 개요

P1/P2/P3 Phase에 따라 화면 목록 필터링 및 화면 내부 요소 표시/숨김을 제어하는 시스템

---

## 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Control Panel                            │
├─────────────────────────────────────────────────────────────┤
│  Phase Toggle    │  화면 필터링 (screens.json phase 기반)   │
│  (P1/P2/P3)      │  → renderNavButtons() 재호출             │
│                  │  → body[data-current-phase] 설정         │
├──────────────────┼──────────────────────────────────────────┤
│  Navigation      │  화면 전환 (navigateTo)                  │
│  (🏠📊🔍...)    │  → 컨트롤러 cleanup/reset/start 호출     │
├──────────────────┼──────────────────────────────────────────┤
│  State Toggle    │  화면별 상태 변경 (loading/error/empty)  │
│  (⏳❌📭...)    │  → updateScreenState() 직접 호출         │
├──────────────────┼──────────────────────────────────────────┤
│  App Control     │  앱 시작/초기화                          │
│  (🚀🔄)          │  → 화면 플로우, isFirstVisit 리셋        │
└──────────────────┴──────────────────────────────────────────┘
```

---

## Phase 토글 동작

| 레이어 | 제어 대상 | 방식 |
|--------|-----------|------|
| 화면 목록 필터 | 네비게이션 버튼 | `screens.json` phase + JS 필터 |
| 요소 표시 토글 | 화면 내부 P2/P3 요소 | `data-phase` + CSS |

### CSS 토글 규칙 (base.css)

```css
/* P1 모드: P2/P3 요소 숨김 */
body[data-current-phase="P1"] [data-phase="P2"],
body[data-current-phase="P1"] [data-phase="P3"] {
    display: none !important;
}

/* P2 모드: P3 요소 숨김, P2 플레이스홀더 숨김 */
body[data-current-phase="P2"] [data-phase="P3"],
body[data-current-phase="P2"] [data-phase-placeholder="P2"] {
    display: none !important;
}

/* P3 모드: 모든 플레이스홀더 숨김 */
body[data-current-phase="P3"] [data-phase-placeholder] {
    display: none !important;
}
```

### HTML 마킹 예시

```html
<!-- P1용 플레이스홀더 (P2에서 숨김) -->
<div class="chart-placeholder" data-phase-placeholder="P2">
    <span>추후 지원 예정</span>
</div>

<!-- P2 실제 차트 (P1에서 숨김) -->
<section class="chart-section" data-phase="P2">
    <div class="candlestick-chart">...</div>
</section>
```

---

## 현재 구현 상태

### ✅ 완료

| 항목 | 파일 |
|------|------|
| `setPhase()`에 `data-current-phase` 추가 | `control-panel.js` |
| `initControlPanel()`에 초기값 설정 | `control-panel.js` |
| Phase 토글 CSS 규칙 | `base.css` |
| 컨트롤러 `start()` 호출 연동 | `navigation.js` |
| State 버튼 → `updateScreenState()` 직접 호출 | `control-panel.js` |

### 구현된 화면 (P1)

| 도메인 | 화면 |
|--------|------|
| **Portfolio** | list, detail, edit |
| **Stock** | search, detail |
| **Rebalancing** | check |
| **Settings** | main, profile-edit |
| **Notification** | center, settings |
| **Auth** | splash, login, profile-input |

---

## 관련 파일

- `core/control-panel.js`: Phase 토글 로직
- `core/navigation.js`: 화면 전환 및 컨트롤러 라이프사이클
- `css/base.css`: Phase 기반 CSS 토글 규칙
- `config/screens.json`: 화면별 phase 설정
