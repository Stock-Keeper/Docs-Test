---
type: ui
phase: P1
screen: 포트폴리오 목록 화면
related:
  api:
    - specs/api/portfolio/list.md
  ui:
    - specs/ui/portfolio/detail.md
  shared:
    - specs/ui/_shared/modal.md
    - specs/ui/_shared/summary-card.md
reference: ../../../reference/pages/home.md
---

# 포트폴리오 목록 화면 (Portfolio List)

## 개요

사용자 포트폴리오 목록 표시 및 관리

## 레이아웃

```text
┌───────────────────────────────────────┐
│ [김] 안녕하세요 👋             [🔔][⚙️] │  ← 홈 헤더 (`home-header`)
│      투자왕김철수 님                  │
├───────────────────────────────────────┤
│ ┌───────────────────────────────────┐ │
│ │ 총 평가금액      업데이트: 09:41  │ │  ← 요약 헤더 (`summary-header`)
│ │ ₩45,230,500                       │ │  ← 총 평가금액 (`summary-value`)
│ │ +₩2,340,200 (+5.47%)              │ │  ← 증감액/수익률 (`summary-change`)
│ │ ───────────────────────────────── │ │
│ │ 💵 보유 현금           ₩1,500,000 │ │  ← 보유 현금 (`cash-section`)
│ └───────────────────────────────────┘ │
├───────────────────────────────────────┤
│ 내 포트폴리오                     3개 │  ← `section-header`
│                                       │
│ ┌───────────────────────────────────┐ │  ← `portfolio-card`
│ │ [색상] 메인 포트폴리오 [리밸런싱필요] › │ │  ← `portfolio-card-header`
│ │        5종목 [🔔][5%]               │ │  ← `portfolio-sub-row`
│ │ ───────────────────────────────── │ │
│ │       평가금액                      │ │  ← `portfolio-card-body`
│ │       ₩32,450,000         +7.23%  │ │
│ └───────────────────────────────────┘ │
│                                       │
│ ┌───────────────────────────────────┐ │
│ │ [색상] 성장주 투자                  › │ │
│ │ ...                                 │ │
│ └───────────────────────────────────┘ │
│                                       │
│                                 [➕]  │  ← 추가 FAB (`portfolio-add-fab`)
└───────────────────────────────────────┘
```

## 컴포넌트

### 1. 헤더 (`.home-header`)
- 구성: 사용자 아바타 (`#user-avatar`), 인사말 ("안녕하세요 👋", `#user-nickname`), 알림 버튼 (`#notification-btn`), 설정 버튼 (`#settings-btn`)
- Phase 2 이후 바텀 네비게이션 도입 시 설정/알림 버튼 진입점 변경 가능

### 2. 요약 카드 (`.home-summary` / `.summary-card`)
| 요소 | 클래스명/ID | 설명 |
|---|---|---|
| 총 평가금액/시간 | `.summary-header` | 업데이트 시각 표출 (`#total-update-time`) |
| 총액 | `.summary-value` / `#total-valuation` | 가장 큰 타이포그래피로 볼드 처리 |
| 증감액/률 | `.summary-change` (`#total-change-container`) | `#total-change-amount`, `#total-change-rate`. `positive`/`negative` 클래스로 색상 부여 |
| 현금 | `.cash-section` | `#total-cash` 하단 분리 표출 |

### 3. 포트폴리오 목록 (`.portfolio-section`)
- 섹션 헤더 (`.section-header`): "내 포트폴리오" 타이틀 및 갯수 (`#portfolio-count`)

#### 3.1. 포트폴리오 카드 하위 구성 (`.portfolio-card`)
| 요소 | 클래스명 | 설명 |
|---|---|---|
| 상단 영역 | `.portfolio-card-header` | 색상, 정보, 배지, 상세 이동 화살표(`.portfolio-arrow`) 포함 영역 |
| 심볼/테마 | `.portfolio-color` | 컬러 그라디언트 아이콘 표출 |
| 종목 요약 | `.portfolio-sub-row` | 종목수(`.portfolio-stocks`), 알림 마크(`.notification-icon`), 임계값(`.threshold-badge`) |
| 리밸런싱 경고 | `.rebalance-badge` | "리밸런싱 필요" 뱃지 (조건 만족 시) |
| 하단 정보 | `.portfolio-card-body` | 밸류에이션 지표 및 수익률 배치 |
| 평가액 | `.portfolio-value` | `.value-label`, `.value-amount` |
| 수익률 | `.portfolio-return` | `positive`/`negative` 로 색상 강조 |

### 4. 스와이프 액션 / 드래그 (명세 확장)
- 왼쪽 스와이프: 수정 (추후 지원)
- 오른쪽 스와이프: 삭제 (추후 지원)
- 롱프레스: 포트폴리오 순서 변경 (추후 지원)

### 5. 생성 플로우 (`#portfolio-add-fab` & `#create-portfolio-modal`)
- FAB(`#portfolio-add-fab`) 클릭 시 하단에서 생성 바텀시트(`#create-portfolio-modal`) 등장
- 바텀시트 입력 항목:
  - 이름 (`#portfolio-name-input`): 20자 제한 (`#name-count`)
  - 설명 (`#portfolio-desc-input`): 100자 제한 (`#desc-count`)
- 버튼: 취소(`#portfolio-modal-cancel-btn`), 만들기(`#portfolio-modal-create-btn`)

## 상호작용

| 액션 | 동작 |
|---|---|
| 헤더 설정 아이콘 탭 | 설정 화면 화면 / 내 정보 화면 진입 |
| 요약 카드 탭 | 보유 및 관심 현황 상세 정보 확인 (향후 구현) |
| 카드 탭 | 해당 포트폴리오 상세 화면(`detail.html`) 진입 |
| FAB 추가 탭 | 생성 Баттом시트(`create-portfolio-modal`) 표시 |
| 바텀시트 '만들기' 탭| 포트폴리오 신규 생성 API 호출 처리 후 목록 추가 |
| (추후) 카드 드래그 | 카드 롱프레스하여 순서 재배치 지원 |
| (추후) 스와이프 | 좌측=수정, 우측=삭제 |
| Pull to Refresh | 전체 포트폴리오 최신 호가 및 수치 새로고침 |

## 상태

| 상태 | UI |
|---|---|
| 로딩 중 | 스켈레톤 카드 |
| 빈 목록 | 요약 카드 숨김, "포트폴리오를 추가해보세요" 빈 화면 표시 |
| 에러 | 에러 메시지 + 재시도 버튼 |
| 5개 도달 | 플로팅 추가 버튼(FAB) 비활성화 또는 숨김 |

## 관련 스펙

- API: `specs/api/portfolio/list.md`
- UI: `specs/ui/portfolio/detail.md`
- UI: `specs/ui/portfolio/create.md`
