---
type: ui
phase: P2
screen: 피드 화면
related:
  api:
    - specs/api/community/feed-list.md
  ui:
    - specs/ui/community/search.md
---

# 피드 화면 (Feed Screen)

## 개요

커뮤니티 메인 피드 - 게시글/포트폴리오 통합 목록

## 레이아웃

```text
┌─────────────────────────────────┐
│  커뮤니티             [🔍] [👤] │
├─────────────────────────────────┤
│    [ 추천 ]        [ 팔로잉 ]   │
├─────────────────────────────────┤
│  [전체 ▼] [최신순 ▼]            │
├─────────────────────────────────┤
│  ┌─────────────────────────────┐│
│  │ 📄 국내주식                 ││
│  │ 삼성전자 분석글             ││
│  │ 내용 미리보기...            ││
│  │ 🖼 2  ❤️ 42  💬 5          ││
│  │ 투자왕 · 2시간 전           ││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │ 📊 포트폴리오      +12.5%   ││
│  │ 배당주 포트폴리오           ││
│  │ 종목 8개 · 배당러버         ││
│  │ ❤️ 156  📋 23              ││
│  │ 1일 전                      ││
│  └─────────────────────────────┘│
│                                 │
│  [ spinner ]                    │
│                                 │
│                              ✏️│
└─────────────────────────────────┘
│  [홈]  [포폴]  [커뮤]  [설정]   │
└─────────────────────────────────┘
```

## 컴포넌트

### 1. 헤더 (`screen-header`)

| 요소 | 클래스/ID / 설명 | Phase |
|---|---|---|
| 타이틀 | `.header-title` / "커뮤니티" | P1 |
| 검색 버튼 | `#feed-search-btn` (`.header-icon-btn`) / 검색 화면 이동 (🔍) | P1 |
| 프로필 버튼 | `#feed-profile-btn` (`.header-icon-btn`) / 내 커뮤니티 프로필 이동 (👤) | P1 |

### 2. 콘텐츠 탭 (`feed-tabs`)

| 탭 (`feed-tab`) | 설명 | Phase |
|---|---|---|
| 추천 (`active`) | 최근 이슈 / 좋아요 많은 글 / 인기 급상승 | P1 |
| 팔로잉 | 내가 팔로우하는 사람들의 글 | P2 |

### 3. 필터 바 (`.feed-filter-bar`)

| 필터 | ID / 설명 | Phase |
|---|---|---|
| 카테고리 | `#feed-category-filter` (`.feed-filter-select`) / 전체, 포트폴리오, 토론, 질문, 국내주식, 해외주식, 자유 | P1 |
| 정렬 | `#feed-sort-filter` (`.feed-filter-select`) / 최신순, 인기순 | P1 |

### 4. 피드 목록 (`#feed-list` / `.feed-list`)

#### 일반 게시글 카드 (`feed-card`)

| 요소 | 설명 | Phase |
|---|---|---|
| 카테고리 배지 (`feed-card-category`) | 말머리 표시 | P1 |
| 제목 (`feed-card-title`) | 1줄, 말줄임 | P1 |
| 미리보기 (`feed-card-preview`) | 2줄, 말줄임 | P1 |
| 통계 및 작성자 (`feed-card-meta`) | 작성자/시간 및 좋아요 수, 댓글 수 등 그룹 영역 | P1 |

#### 포트폴리오 게시글 카드 (`.feed-card.portfolio-type`)

| 요소 | 설명 | Phase |
|---|---|---|
| 카테고리 배지 (`.feed-card-category`) | 📊 포트폴리오 | P1 |
| 수익률 (`.feed-card-change`) | 포트폴리오 총 수익률 표기 (우측 상단, 증감 색상 적용 `positive` / `negative`) | P1 |
| 제목 (`.feed-card-title`) | 포트폴리오명 | P1 |
| 미리보기 (`.feed-card-preview`) | 종목 N개 · 작성자 설명 등 | P1 |
| 하단 메타 (`.feed-card-meta`) | 컴포넌트 내부 통계 영역 (`.feed-card-stats`) 포함 | P1 |

### 5. 추가 UI 요소

| 요소 | ID/클래스 | 설명 | Phase |
|---|---|---|---|
| FAB | `#feed-write-fab` (`.fab`) | 게시글 작성 플로팅 버튼 (✏️) | P1 |
| 스켈레톤 | `#community-feed-skeleton` | 로딩 시 스켈레톤 카드(`skeleton-card`) 표시 | P1 |
| 빈 피드 안내 | `#community-feed-empty-state` | 게시글 없음 아이콘/안내 | P1 |
| 첫 글 쓰기 버튼 | `#feed-empty-write-btn` (`.empty-btn`) | 빈 피드 안의 작성 버튼 | P1 |
| 무한 스크롤 트리거 | `#feed-infinite-trigger` | 하단 도달 시 자동 로드 및 스피너(`.spinner-sm`) 노출 | P1 |

## 상호작용

| 액션 | 동작 | Phase |
|---|---|---|
| 카드 탭 | 게시글 상세 / 포트폴리오 상세 화면 이동 | P1 |
| 좋아요 탭 | 좋아요 API 연동 필요 | P2 |
| FAB 탭 | 게시글 작성 화면 이동 | P1 |
| 탭 클릭 (추천/팔로잉) | 피드 리스트 변경 | P1 |
| 필터 변경 | 목록 필터링 재조회 | P1 |
| Pull to Refresh (예정) | 상단 스와이프 시 최신 데이터 로드 | P2 |
| 무한 스크롤 Trigger | 스크롤 하단 도달 시 추가 데이터 로드 | P1 |

## 본인인증 미완료 시

- 조회는 가능
- 좋아요, 댓글, 작성 클릭 시 → "본인인증이 필요합니다" 모달 → 인증 화면 이동

## 상태

| 상태 | UI | Phase |
|---|---|---|
| 로딩 중 | 스켈레톤 카드(`community-feed-skeleton`) 노출 | P1 |
| 빈 피드 | `community-feed-empty-state` 노출 | P1 |
| 추가 데이터 로딩 | 하단 무한 스크롤 트리거 영역 내 스피너 노출 | P1 |

## 관련 스펙

- API: `specs/api/community/feed-list.md`
- UI: `specs/ui/community/post-detail.md`
- UI: `specs/ui/community/search.md`
