---
type: ui
phase: P1
screen: 알림 센터 화면
related:
  api:
    - ../../api/notification/list.md
    - ../../api/notification/read.md
---

# 알림 센터 화면 (Notification Center)

## 개요
알림 목록 표시 및 관리

## 레이아웃

```text
┌───────────────────────────────────────┐
│  ← 알림                       전체 읽음 │  ← 헤더 (`screen-header`)
├───────────────────────────────────────┤
│  오늘만 진행하는 특별 혜택 이벤트! ➔    │  ← 상단 배너 (`ad-banner noti-sticky`)
├───────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │ 🔔 [내 포트폴리오] 리밸런싱   ●│  │  ← 알림 아이템 (`notification-item unread`)
│  │    3개 종목 비율 교체 필요      │  │
│  │    2시간 전                     │  │
│  └───────────────────────────────┘  │
│                                       │
│  ┌───────────────────────────────┐  │
│  │ 🔔 [배당주] 리밸런싱            │  │  ← 읽은 알림 (`notification-item`)
│  │    2개 종목 매수 조건 달성      │  │
│  │    어제                         │  │
│  └───────────────────────────────┘  │
└───────────────────────────────────────┘
```

## 컴포넌트

### 1. 헤더 (`screen-header`)
- 뒤로가기 (`noti-center-back-btn`)
- 타이틀: "알림" (`header-title`)
- 전체 읽음 버튼 (`noti-read-all-btn`): 텍스트 형태 (`text-btn`), 모든 알림 읽음 처리

### 2. 알림 목록 (`notification-list`) 및 아이템 (`notification-item`)
| 요소 | 설명 | Phase |
|---|---|---|
| 상단 배너 | 고정형 프로모션/공지 배너 (`ad-banner noti-sticky`) | P2 |
| 아이콘 | 🔔 알림 유형별 아이콘 (`noti-icon-box`) | P1 |
| 제목 | [포트폴리오명] 등 메인 타이틀 (`noti-title`) | P1 |
| 읽음 표시 | 미읽음일 때 파란 점 노출 (`noti-dot`, 컨테이너에 `unread` 클래스) | P1 |
| 내용 | 상세 메시지 (`noti-message`) | P1 |
| 시간 | 상대 시간 (`noti-time`) | P1 |
| 삭제버튼 | 스와이프 or 호버 시 노출되는 삭제 액션 (`noti-delete-btn`) | P1 |

### 3. 스와이프 액션
- 왼쪽 스와이프: 삭제 버튼 노출

## 상호작용

| 액션 | 동작 |
|------|------|
| 알림 카드 탭 | 해당 포트폴리오로 이동 + 읽음 처리 |
| 전체 읽음 탭 | 모든 알림 읽음 처리 |
| 왼쪽 스와이프 | 삭제 버튼 노출 |
| 삭제 버튼 탭 | 해당 알림 삭제 |
| Pull to Refresh | 최신 알림 로드 |

## 상태

| 상태 | 컨테이너 | UI |
|---|---|---|
| 로딩 중 | `noti-skeleton` | 스켈레톤 리스트 (`skeleton-container`) 5개 항목 반복 표시 |
| 알림 없음 | `noti-empty-state` | 아이콘 (`📭`), "새로운 알림이 없습니다" 텍스트 |
| 정상 목록 | `notification-list` | `notification-item` 요소 반복 표시 |
| 에러 | - | 에러 메시지 + 재시도 버튼 (스낵바 혹은 UI 표출) |

## 관련 스펙
- API: `../api/notification/list.md`
- API: `../api/notification/read.md`
