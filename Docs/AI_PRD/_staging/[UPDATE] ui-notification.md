---
source: _inbox/pages/07_settings.md
type: ui
domain: notification
intent: update
---

# [UPDATE] Notification Settings UI

## 1. 개요

`_inbox/pages/07_settings.md`의 새로운 설정 페이지 디자인에 맞춰, 알림 설정(`specs/ui/notification/settings.md`)의 스펙을 동기화합니다. 기존에는 별도의 독립된 화면으로 정의되었으나, 새로운 디자인에서는 **설정(Settings) 통합 화면 내의 섹션**으로 변경되었습니다.

## 2. 변경 사항 요약

- **[MODIFY] specs/ui/notification/settings.md**: 독립 `Screen` 정의를 `Section` 정의로 변경하고, 포트폴리오별 설정 기능을 제외(각 포트폴리오 상세에서 관리)하여 복잡도를 낮춥니다.

## 3. 상세 스펙 제안

### A. [MODIFY] specs/ui/notification/settings.md

```markdown
# 알림 설정 (Notification Settings)

## 개요
앱 설정 화면(`specs/ui/settings.md`) 내의 **알림 설정 섹션**에 대한 상세 동작 정의

## UI 컨텍스트 (In Settings Screen)

```
설정 (Screen)
 └─ 알림 설정 (Section)
     ├─ 푸시 알림 [토글]        (Global ON/OFF)
     ├─ 알림 주기  매일 >       (Frequency Selector)
     └─ 알림 시간  16:00 >      (Time Picker)
```

## 컴포넌트 상세

### 1. 푸시 알림 (Global Toggle)
- **UI**: 스위치 (Switch)
- **동작**:
  - ON: 기기 푸시 권한 확인 후 활성화 (권한 없을 시 시스템 설정 유도 다이얼로그)
  - OFF: 모든 알림 비활성화 (하위 메뉴 '알림 주기', '알림 시간' 비활성화 처리)

### 2. 알림 주기 (Frequency)
- **UI**: 셀렉트 메뉴 (List Tile with chevron)
- **옵션**:
  - 매일 (Daily)
  - 매주 (Weekly) - 월요일 기준
  - 2주마다 (Bi-weekly)
  - 한 달마다 (Monthly) - 1일 기준
- **인터랙션**: 탭 시 **바텀 시트(Bottom Sheet)** 호출하여 선택

### 3. 알림 시간 (Time)
- **UI**: 시간 표시 (HH:MM)
- **기본값**: 16:00 (장 마감 후)
- **인터랙션**: 탭 시 **타임 피커(Time Picker)** 호출 (시스템 네이티브 피커)

## 제외된 기능 (Changed from P1)
- **포트폴리오별 개별 설정**: 
  - 기존: 알림 설정 화면에서 리스트로 관리
  - **변경**: 각 포트폴리오 상세 화면(`specs/ui/portfolio/detail.md`)의 [편집/설정] 메뉴로 이동했습니다. 전체 설정 화면에서는 전역(Global) 설정만 다룹니다.

## 관련 스펙
- Parent UI: `specs/ui/settings.md` (07_settings.md)
- API: `specs/api/notification/settings.md`
```

## 4. AI 분석 (Chain of Thought)

1.  **UX 단순화**: 알림 설정이 복잡한 Depth를 가지는 것보다, 설정 메인 화면에서 직관적으로 제어할 수 있도록 변경된 `07_settings.md`의 의도를 반영했습니다.
2.  **포트폴리오 설정 이관**: "포트폴리오별 알림"은 해당 포트폴리오를 보고 있을 때 설정하는 것이 더 자연스러운 맥락(Contextual)입니다. 글로벌 설정에서는 제거하여 화면을 깔끔하게 유지합니다.
3.  **구현 용이성**: API(`specs/api/notification/settings.md`)는 그대로 유지하되, UI 호출 위치만 분산(Global Setting vs Portfolio Detail)되는 구조입니다.

## 5. 🔍 확인 필요 사항

### 1. 포트폴리오별 설정 제거 확인
전체 알림 설정 화면에서 "개별 포트폴리오 알림 끄기" 기능을 제거하고, 이를 각 포트폴리오 상세 화면으로 위임하는 것에 동의하십니까?

- [ ] **동의 (상세 화면에서 관리)**: 현재 제안
- [ ] **유지 (전체 화면에서도 관리)**: "상세 설정" 하위 메뉴를 만들어 진입하도록 수정 필요
